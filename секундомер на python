from time import time
def sec():
    count = 0
    time_now = time()
    while True:
        a = time() - time_now
        if not a % 1:
            print(count)
            print(a)
            count += 1
sec()
